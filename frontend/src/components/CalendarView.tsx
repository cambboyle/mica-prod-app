import React from 'react';
import { Box, Typography } from '@mui/material';

const CalendarView: React.FC = () => {
  return (
    <Box>
      <Typography>Calendar Coming Soon</Typography>
    </Box>
  );
};

export default CalendarView;